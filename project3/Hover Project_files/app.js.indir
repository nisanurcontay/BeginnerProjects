const ball = document.querySelector("#ball");

ball.addEventListener("mouseenter", () => {
  if (!ball.classList.contains("hover")) {
    ball.classList.add("hover");
  }
});

ball.addEventListener("mouseleave", () => {
  if (ball.classList.contains("hover")) {
    ball.classList.remove("hover");
  }
});
